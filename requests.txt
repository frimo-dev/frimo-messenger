curl -i \
  -X POST http://localhost/auth/register \
  -H 'Content-Type: application/json' \
  -d '{
    "email": "test@example.com",
    "password": "TestPassword123!"
  }'

curl -i \
  -X POST http://localhost/auth/resend \
  -H 'Content-Type: application/json' \
  -d '{
    "email": "test@example.com"
  }'

curl -i "http://localhost/auth/verify-email?token=${TOKEN}"